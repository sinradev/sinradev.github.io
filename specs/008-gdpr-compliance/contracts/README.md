# Contracts: GDPR Compliance

**N/A - Static site, no API endpoints.**

This feature modifies only:
- Static HTML templates (layout files)
- Static content files (markdown)
- Static configuration (YAML data files)
- Static assets (font files, CSS)

No REST endpoints, GraphQL schemas, or API contracts apply.

The external dependency `app.sinra.dev/contact/form` (contact form POST target) is an existing endpoint not modified by this feature.
